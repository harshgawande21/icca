{
  "name": "icca-backend",
  "version": "1.0.0",
  "description": "Backend API for Intelligent Client Communication Assistant",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node migrations/migrate.js",
    "setup-db": "node setup-database.js",
    "test-db": "node -e \"require('./database/connection.js').query('SELECT NOW()').then(r => console.log('✅ Database connected:', r.rows[0].now))\"",
    "start:free": "node --max-old-space-size=512 server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}